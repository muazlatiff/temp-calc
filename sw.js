var GHPATH = '/temp-calc';
var APP_PREFIX = 'gppwa_';
 
// every time any of the files change, need to change this version (version_01, version_02…)
var VERSION = 'version_02';

var URLS = [    
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/index.js`,
  `${GHPATH}/ade-logo-white.png`,
];
